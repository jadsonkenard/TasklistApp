import { HeaderContainer, HeaderTitle } from "./styles";

export const Header = () => {
    return(
        <HeaderContainer>
            <HeaderTitle>Tasks</HeaderTitle>
        </HeaderContainer>
    )
}